# Pinned so a dependency release can't silently restyle or break the dev
# channel (frozen release snapshots never rebuild, so an unpinned dev drifts
# away from them -- e.g. the shibuya 2025+ link-underline change). Bump these
# deliberately and eyeball the dev build after.
sphinx==8.2.3
shibuya==2026.7.12
sphinx-llms-txt==0.7.1
sphinxcontrib-video==0.4.2
